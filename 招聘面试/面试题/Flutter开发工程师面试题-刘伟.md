# Flutter 开发工程师面试题 — 刘伟

> 候选人：刘伟 | 3年工作经验（Flutter 约1.5年）| Flutter/GetX 方向
> 简历日期：2026-03-12 | 面试题整理日期：2026-03-13
> 参考答案见：[Flutter开发工程师参考答案-刘伟.md](Flutter开发工程师参考答案-刘伟.md)

---

## 候选人画像

- 25岁，西南交通大学本科（信息管理与信息系统）
- Flutter 实际经验约1.5年（2024.07-2025.11，Techvify）
- 之前是数据开发工程师（SQL/ETL），属于跨方向转型
- 技术栈：Flutter/Dart + GetX + 支付宝/微信支付 SDK
- 项目：游戏集成平台（GetX全家桶）+ 游戏代付充值平台（支付对接）
- 求职意向写的是 Android，但投的是 Flutter 岗 ⚠️ 需要当面确认

---

## 面试前确认

> 开场先问，避免方向不匹配浪费时间

- 你简历上求职意向写的是 Android，但投的是 Flutter 岗，你目前的主要方向是哪个？
- 在 Techvify 离职的原因是什么？（2025.11 离职，距今约4个月）

---

## 一、Flutter 基础原理

> 目标：区分"会用框架"和"理解原理"，3年经验候选人基础要扎实

1. Flutter 的 Widget 分为 StatelessWidget 和 StatefulWidget，你在项目中如何决定用哪种？
2. StatefulWidget 的生命周期有哪些？`setState` 调用后发生了什么？
3. Flutter 的 `BuildContext` 是什么？为什么有时候会遇到 "context is no longer valid" 的错误？
4. `const` 构造函数在 Flutter 中有什么作用？你在项目中有没有刻意使用过？
5. Flutter 的渲染流程是什么？Widget / Element / RenderObject 各自的职责是什么？

---

## 二、GetX 深度考察

> 他的核心框架，必须考深，区分"会用"和"真懂"

1. GetX 的状态管理有几种方式？`GetBuilder`、`Obx`、`GetX Widget` 的区别是什么？你在项目中用的是哪种，为什么？
2. GetX 的依赖注入是怎么工作的？`Get.put`、`Get.lazyPut`、`Get.find` 的区别？什么时候会出现找不到依赖的问题？
3. 【追问简历】你提到用 GetX 完成路由管理，GetX 的命名路由和普通路由有什么区别？路由传参是怎么做的？
4. GetX 的 Controller 生命周期是怎么管理的？`onInit`、`onReady`、`onClose` 分别在什么时候调用？Controller 什么时候会被销毁？
5. GetX 和 Provider、Riverpod 相比，你觉得 GetX 的缺点是什么？

---

## 三、支付对接（追问简历）

> 他的核心项目亮点，要考察是否真正主导过，还是只是调用 SDK

1. 【追问简历】你提到支付调起成功率达99.8%，这个数据是怎么统计的？剩下0.2%的失败是什么原因？
2. 对接微信/支付宝 SDK 时，支付结果的回调是怎么处理的？如果用户支付成功但回调没收到，怎么处理？
3. 订单状态同步的逻辑是怎么设计的？客户端轮询还是服务端推送？
4. 支付过程中如何防止重复提交？用户快速点击两次支付按钮怎么处理？
5. 【追问简历】你提到"支付重试"逻辑，具体是怎么实现的？重试的触发条件是什么？

---

## 四、Flutter 性能与工程化

> 考察是否有超出"完成功能"的工程意识

1. 【追问简历】你提到实现了"图片缓存"，用的是什么方案？`cached_network_image` 还是自己实现？有没有遇到过内存问题？
2. 【追问简历】你提到"页面性能优化"，具体做了哪些优化？怎么发现性能问题的？
3. Flutter 列表（ListView）性能优化有哪些手段？`ListView.builder` 和 `ListView` 的区别？
4. 【追问简历】你提到负责"多环境配置"，是怎么实现的？开发/测试/生产环境的切换是怎么做的？
5. 你们项目的打包构建流程是怎样的？有没有用 CI/CD？

---

## 五、Dart 语言基础

> 基础语言能力，3年经验应该扎实

1. Dart 的 `async/await` 和 `Future` 是什么关系？`then` 和 `await` 有什么区别？
2. Dart 中 `??`、`?.`、`!` 操作符分别是什么意思？什么时候会用到 `!`，有什么风险？
3. Dart 的 `Stream` 是什么？和 `Future` 的区别？你在项目中有没有用过 Stream？
4. Dart 中如何处理异常？`try/catch` 和 `catchError` 的区别？

---

## 六、职业发展与背景核实

> 了解候选人的真实情况和成长潜力

1. 你之前是做数据开发的，为什么转到移动端？这个转变是怎么发生的？
2. 在 Techvify 工作期间，你觉得自己成长最大的地方是什么？有没有遇到过解决不了的技术问题？
3. 你目前 Flutter 的薄弱点在哪里？接下来打算怎么提升？
4. 如果让你独立负责一个新的 Flutter 项目从零开始，你会怎么搭建项目架构？

---

## 面试策略备注

- **先确认方向**：求职意向写 Android 但投 Flutter 岗，开场必须澄清
- **GetX 是核心**：他所有项目都用 GetX，这块要考深，区分"会用"和"真懂"
- **支付经验要追问**：99.8% 成功率这种数据要核实，看是否真正主导过
- **转型背景要关注**：从数据开发转 Flutter，Flutter 实际经验只有1.5年，基础可能有短板
- **不要考 iOS 原生**：他没有 iOS 背景，不要问 RunLoop/Runtime 这类题
- **重点评估潜力**：3年经验中只有1.5年 Flutter，考察学习能力和成长潜力比考察深度更重要
